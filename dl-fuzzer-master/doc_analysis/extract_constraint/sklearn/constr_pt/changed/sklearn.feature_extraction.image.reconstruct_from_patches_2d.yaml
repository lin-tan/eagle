constraints:
  image_size:
    descp: tuple of ints (image_height, image_width) or (image_height, image_width,
      n_channels) the size of the image that will be reconstructed
    dtype:
    - numeric
    ndim:
    - '1'
    range:
    - '[0,inf)'
  patches:
    descp: 'array, shape = (n_patches, patch_height, patch_width) or (n_patches, patch_height,
      patch_width, n_channels) The complete set of patches. If the patches contain
      colour information, channels are indexed along the last dimension: RGB patches
      would have `n_channels=3`.'
inputs:
  optional: []
  required:
  - patches
  - image_size
link: ''
package: scikit-learn
target: reconstruct_from_patches_2d
title: sklearn.feature_extraction.image.reconstruct_from_patches_2d
version: 0.24.X
