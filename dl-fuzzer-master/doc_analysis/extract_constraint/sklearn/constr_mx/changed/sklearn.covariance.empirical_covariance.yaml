constraints:
  X:
    descp: ndarray, shape (n_samples, n_features) Data from which to compute the covariance
      estimate
  assume_centered:
    default: 'False'
    descp: boolean If True, data will not be centered before computation. Useful when
      working with data whose mean is almost, but not exactly zero. If False, data
      will be centered before computation.
    dtype:
    - boolean
    ndim:
    - '0'
inputs:
  optional:
  - assume_centered
  required:
  - X
link: ''
package: scikit-learn
target: empirical_covariance
title: sklearn.covariance.empirical_covariance
version: 0.24.X
