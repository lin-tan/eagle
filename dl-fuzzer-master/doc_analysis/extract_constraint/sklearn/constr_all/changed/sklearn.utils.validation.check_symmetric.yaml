constraints:
  array:
    descp: 'nd-array or sparse matrix Input object to check / convert. Must be two-dimensional
      and square, otherwise a ValueError will be raised. tol : float Absolute tolerance
      for equivalence of arrays. Default = 1E-10. raise_warning : boolean (default=True)
      If True then raise a warning if conversion is required. raise_exception : boolean
      (default=False) If True then raise an exception if array is not symmetric.'
    dtype:
    - boolean
    ndim:
    - '0'
  raise_exception:
    default: 'False'
    descp: ''
    dtype:
    - boolean
    ndim:
    - '0'
  raise_warning:
    default: 'True'
    descp: ''
    dtype:
    - boolean
    ndim:
    - '0'
  tol:
    default: 1e-10
    descp: ''
    dtype:
    - float
    ndim:
    - '0'
inputs:
  optional:
  - tol
  - raise_warning
  - raise_exception
  required:
  - array
link: ''
package: sklearn
target: check_symmetric
title: sklearn.utils.validation.check_symmetric
version: 0.24.2
