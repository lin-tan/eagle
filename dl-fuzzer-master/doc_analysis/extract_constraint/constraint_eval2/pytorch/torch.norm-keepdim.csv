descp1,descp2,default,attribute,constraint,in_doc,extracted
bool,whether the output tensors have `dim` retained or not. Ignored if `dim` = `None` and `out` = `None`. Default: `False`,False,prim_dtype,dtype(torch.bool),,1
bool,whether the output tensors have `dim` retained or not. Ignored if `dim` = `None` and `out` = `None`. Default: `False`,False,shape,ndim(0),,1
