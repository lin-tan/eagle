descp1,descp2,default,attribute,constraint,in_doc,extracted
List[Tensor],Input and output GPU tensors of the collective. The function operates in-place. You also need to make sure that `len(tensor_list)` is the same for all the distributed processes calling this function.,,nonprim_dtype,"tensor_t(torch.tensor), structure(list(torch.tensor))",,1
