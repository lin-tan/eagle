descp1,descp2,default,attribute,constraint,in_doc,extracted
,"Integer, the axis that should be normalized (typically the features axis). For instance, after a `Conv2D` layer with`data_format=""channels_first""`, set `axis=1` in `BatchNormalization`.",-1,prim_dtype,dtype(int),,1
,"Integer, the axis that should be normalized (typically the features axis). For instance, after a `Conv2D` layer with`data_format=""channels_first""`, set `axis=1` in `BatchNormalization`.",-1,shape,ndim(0),,1
