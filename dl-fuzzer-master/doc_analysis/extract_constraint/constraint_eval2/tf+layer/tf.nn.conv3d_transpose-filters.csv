descp1,descp2,default,attribute,constraint,in_doc,extracted
,"A 5-D `Tensor` with the same type as `value` and shape `[height, width, output_channels, in_channels]`.  `filter`'s `in_channels` dimension must match that of `value`.",,nonprim_dtype,tensor_t(tf.tensor),,1
,"A 5-D `Tensor` with the same type as `value` and shape `[height, width, output_channels, in_channels]`.  `filter`'s `in_channels` dimension must match that of `value`.",,shape,"shape([height,width,output_channels,in_channels]), ndim(4, 5)",,1
