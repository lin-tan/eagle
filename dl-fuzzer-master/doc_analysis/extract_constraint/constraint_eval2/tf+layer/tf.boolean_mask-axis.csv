descp1,descp2,default,attribute,constraint,in_doc,extracted
,"A 0-D int Tensor representing the axis in `tensor` to mask from. By default, axis is 0 which will mask from the first dimension. Otherwise K + axis <= N.",None,prim_dtype,dtype(int),,1
,"A 0-D int Tensor representing the axis in `tensor` to mask from. By default, axis is 0 which will mask from the first dimension. Otherwise K + axis <= N.",None,nonprim_dtype,tensor_t(tf.tensor),,1
,"A 0-D int Tensor representing the axis in `tensor` to mask from. By default, axis is 0 which will mask from the first dimension. Otherwise K + axis <= N.",None,shape,ndim(0),,1
