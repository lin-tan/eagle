descp1,descp2,default,attribute,constraint,in_doc,extracted
,"If True, multiply by `gamma`. If False, `gamma` is not used. When the next layer is linear (also e.g. `nn.relu`), this can be disabled since the scaling will be done by the next layer.",True,prim_dtype,dtype(tf.bool),,1
,"If True, multiply by `gamma`. If False, `gamma` is not used. When the next layer is linear (also e.g. `nn.relu`), this can be disabled since the scaling will be done by the next layer.",True,shape,ndim(0),,1
