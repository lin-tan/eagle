descp1,descp2,default,attribute,constraint,in_doc,extracted
,"Number of regression targets per example. This is the size of the last dimension of the labels and logits `Tensor` objects (typically, these have shape `[batch_size, label_dimension]`).",1,prim_dtype,dtype(int),,1
,"Number of regression targets per example. This is the size of the last dimension of the labels and logits `Tensor` objects (typically, these have shape `[batch_size, label_dimension]`).",1,shape,"shape([batch_size,&label_dimension]), ndim(0, 2)",,1
,"Number of regression targets per example. This is the size of the last dimension of the labels and logits `Tensor` objects (typically, these have shape `[batch_size, label_dimension]`).",1,validvalue,"range([0,inf))",,1
