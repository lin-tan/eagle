descp1,descp2,default,attribute,constraint,in_doc,extracted
,"A string, either `'VALID'` or `'SAME'`. The padding algorithm. See the ""returns"" section of `tf.nn.convolution` for details.",SAME,prim_dtype,dtype(tf.string),,1
,"A string, either `'VALID'` or `'SAME'`. The padding algorithm. See the ""returns"" section of `tf.nn.convolution` for details.",SAME,shape,ndim(0),,1
,"A string, either `'VALID'` or `'SAME'`. The padding algorithm. See the ""returns"" section of `tf.nn.convolution` for details.",SAME,validvalue,"enum(SAME, VALID)",,1
