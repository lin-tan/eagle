descp1,descp2,default,attribute,constraint,in_doc,extracted
,A potentially ragged boolean tensor.  `mask`'s shape must be a prefix of `data`'s shape.  `rank(mask)` must be known statically.,,prim_dtype,dtype(tf.bool),,1
,A potentially ragged boolean tensor.  `mask`'s shape must be a prefix of `data`'s shape.  `rank(mask)` must be known statically.,,nonprim_dtype,tensor_t(tf.tensor),,1
