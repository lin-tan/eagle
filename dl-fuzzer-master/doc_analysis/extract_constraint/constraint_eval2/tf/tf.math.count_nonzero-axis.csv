descp1,descp2,default,attribute,constraint,in_doc,extracted
,"The dimensions to reduce. If `None` (the default), reduces all dimensions. Must be in the range `[-rank(input), rank(input))`.",None,prim_dtype,dtype(int),,1
,"The dimensions to reduce. If `None` (the default), reduces all dimensions. Must be in the range `[-rank(input), rank(input))`.",None,shape,ndim(1),,1
,"The dimensions to reduce. If `None` (the default), reduces all dimensions. Must be in the range `[-rank(input), rank(input))`.",None,validvalue,"range([-ndim:&input,ndim:&input))",,1
