descp1,descp2,default,attribute,constraint,in_doc,extracted
,Optional. A `Tensor` or list of tensors the same size as`ys` and holding the gradients computed for each y in `ys`.,None,nonprim_dtype,structure(list(tf.tensor)),,1
,Optional. A `Tensor` or list of tensors the same size as`ys` and holding the gradients computed for each y in `ys`.,None,shape,shape(shape:&ys),,1
