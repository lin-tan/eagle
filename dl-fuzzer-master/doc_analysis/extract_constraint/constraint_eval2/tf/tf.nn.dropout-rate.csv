descp1,descp2,default,attribute,constraint,in_doc,extracted
,"A scalar `Tensor` with the same type as x. The probability that each element is dropped. For example, setting rate=0.1 would drop 10% of input elements.",,prim_dtype,dtype(dtype:&x),,1
,"A scalar `Tensor` with the same type as x. The probability that each element is dropped. For example, setting rate=0.1 would drop 10% of input elements.",,nonprim_dtype,tensor_t(tf.tensor),,1
,"A scalar `Tensor` with the same type as x. The probability that each element is dropped. For example, setting rate=0.1 would drop 10% of input elements.",,shape,ndim(0),,1
