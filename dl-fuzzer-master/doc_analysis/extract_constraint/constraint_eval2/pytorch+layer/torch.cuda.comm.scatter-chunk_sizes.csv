descp1,descp2,default,attribute,constraint,in_doc,extracted
Iterable[int],"sizes of chunks to be placed on each device. It should match `devices` in length and sum to `tensor.size(dim)`. If not specified, the tensor will be divided into equal chunks.",None,prim_dtype,dtype(int),,1
Iterable[int],"sizes of chunks to be placed on each device. It should match `devices` in length and sum to `tensor.size(dim)`. If not specified, the tensor will be divided into equal chunks.",None,nonprim_dtype,structure(list(int)),,1
Iterable[int],"sizes of chunks to be placed on each device. It should match `devices` in length and sum to `tensor.size(dim)`. If not specified, the tensor will be divided into equal chunks.",None,shape,ndim(1),,1
