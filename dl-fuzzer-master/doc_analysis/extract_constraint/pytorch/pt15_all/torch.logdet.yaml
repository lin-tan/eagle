constraints:
  input:
    descp: the input tensor of size `(*, n, n)` where `*` is zero or more batch dimensions.
    doc_dtype:
    - Tensor
    ndim:
    - '>=2'
    shape:
    - '[...,n,n]'
    tensor_t:
    - torch.tensor
dependency:
- n
inputs:
  optional: []
  required:
  - input
link: https://pytorch.org/docs/stable/torch.html#torch.logdet
package: torch
ret_type: Tensor
target: logdet
title: torch.logdet
version: 1.5.0
