constraints:
  device:
    default: None
    descp: selected device. Returns statistic for the current device, given by `current_device()`,
      if `device` is `None` (default).
    doc_dtype:
    - torch.device
    - int
    dtype:
    - int
    sig_dtype: Union[torch.device,str,None,int]
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/1.7.0/cuda.html#torch.cuda.reset_max_memory_cached
package: torch
ret_type: None
target: reset_max_memory_cached
title: torch.cuda.reset_max_memory_cached
version: 1.7.0
