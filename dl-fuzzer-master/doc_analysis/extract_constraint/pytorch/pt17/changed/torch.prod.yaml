constraints:
  dtype:
    default: None
    descp: 'the desired data type of returned tensor.If specified, the input tensor
      is casted to `dtype` before the operationis performed. This is useful for preventing
      data type overflows. Default: None.'
    doc_dtype:
    - torch.dtype
    dtype:
    - torch.dtype
    tensor_t:
    - torch.tensor
  input:
    descp: the input tensor.
    doc_dtype:
    - Tensor
    tensor_t:
    - torch.tensor
inputs:
  keyword_only:
  - dtype
  optional:
  - dtype
  required:
  - input
link: https://pytorch.org/docs/1.7.0/generated/torch.prod.html#torch.prod#torch.prod
package: torch
ret_type: Tensor
target: prod
title: torch.prod
version: 1.7.0
