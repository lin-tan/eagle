constraints:
  module:
    descp: module containing the tensor to prune.
    doc_dtype:
    - nn.Module
  name:
    descp: parameter name within `module` on which pruning will act.
    doc_dtype:
    - str
    dtype:
    - string
    ndim:
    - '0'
inputs:
  optional: []
  required:
  - module
  - name
link: https://pytorch.org/docs/1.7.0/generated/torch.nn.utils.prune.identity.html#torch.nn.utils.prune.identity#torch.nn.utils.prune.identity
package: torch
target: identity
title: torch.nn.utils.prune.identity
version: 1.7.0
