constraints:
  seed:
    descp: The desired seed. Value must be within the inclusive range [-0x8000_0000_0000_0000,
      0xffff_ffff_ffff_ffff]. Otherwise, a RuntimeError is raised. Negative inputs
      are remapped to positive values with the formula 0xffff_ffff_ffff_ffff + seed.
    doc_dtype:
    - int
    dtype:
    - int
inputs:
  optional: []
  required:
  - seed
link: https://pytorch.org/docs/1.7.0/generated/torch.manual_seed.html#torch.manual_seed#torch.manual_seed
package: torch
ret_type: torch._C.Generator
target: manual_seed
title: torch.manual_seed
version: 1.7.0
