constraints:
  futures:
    descp: a list of `Future` objects.
    doc_dtype:
    - list
    sig_dtype: List[torch.jit.Future]
    structure:
    - list
inputs:
  optional: []
  required:
  - futures
link: https://pytorch.org/docs/1.7.0/futures.html#torch.futures.collect_all
package: torch
ret_type: torch.futures.Future[List[torch.jit.Future]]
target: collect_all
title: torch.futures.collect_all
version: 1.7.0
