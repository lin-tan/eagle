constraints:
  dim:
    default: '0'
    descp: the dimension over which the tensors are concatenated
    doc_dtype:
    - int
    dtype:
    - int
    ndim:
    - '0'
  out:
    default: None
    descp: the output tensor.
    doc_dtype:
    - Tensor
    tensor_t:
    - torch.tensor
  tensors:
    descp: any python sequence of tensors of the same type. Non-empty tensors provided
      must have the same shape, except in the cat dimension.
    doc_dtype:
    - sequence of Tensors
    structure:
    - sequence(torch.tensor)
    tensor_t:
    - torch.tensor
inputs:
  keyword_only:
  - out
  optional:
  - dim
  - out
  required:
  - tensors
link: https://pytorch.org/docs/1.7.0/generated/torch.cat.html#torch.cat#torch.cat
package: torch
ret_type: Tensor
target: cat
title: torch.cat
version: 1.7.0
