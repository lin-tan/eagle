constraints:
  device:
    default: cuda
    descp: 'The device to return the RNG state of. Default: `''cuda''` (i.e., `torch.device(''cuda'')`,
      the current CUDA device).'
    doc_dtype:
    - torch.device
    - int
    dtype:
    - int
    sig_dtype: Union[int,str,torch.device]
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/1.6.0/cuda.html#torch.cuda.get_rng_state
package: torch
ret_type: torch.Tensor
target: get_rng_state
title: torch.cuda.get_rng_state
version: 1.6.0
