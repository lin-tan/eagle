constraints:
  futures:
    descp: a list of `Future` object.
    doc_dtype:
    - list
    structure:
    - list
inputs:
  optional: []
  required:
  - futures
link: https://pytorch.org/docs/1.6.0/futures.html#torch.futures.wait_all
package: torch
target: wait_all
title: torch.futures.wait_all
version: 1.6.0
