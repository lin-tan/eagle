constraints:
  dim:
    descp: the dimension to reduce. If `None`, the argmin of the flattened input is
      returned.
    doc_dtype:
    - int
    dtype:
    - int
    ndim:
    - '0'
  input:
    descp: the input tensor.
    doc_dtype:
    - Tensor
    tensor_t:
    - torch.tensor
  keepdim:
    default: 'False'
    descp: whether the output tensor has `dim` retained or not. Ignored if `dim=None`.
    doc_dtype:
    - bool
    dtype:
    - torch.bool
    ndim:
    - '0'
inputs:
  optional:
  - keepdim
  required:
  - input
  - dim
link: https://pytorch.org/docs/1.7.0/generated/torch.argmin.html#torch.argmin#torch.argmin
package: torch
ret_type: LongTensor
target: argmin
title: torch.argmin
version: 1.9.0
