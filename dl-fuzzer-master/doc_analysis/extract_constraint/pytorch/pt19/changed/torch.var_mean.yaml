constraints:
  input:
    descp: the input tensor.
    doc_dtype:
    - Tensor
    tensor_t:
    - torch.tensor
  unbiased:
    default: 'True'
    descp: whether to use the unbiased estimation or not
    doc_dtype:
    - bool
    dtype:
    - torch.bool
    ndim:
    - '0'
inputs:
  optional:
  - unbiased
  required:
  - input
link: https://pytorch.org/docs/1.7.0/generated/torch.var_mean.html#torch.var_mean#torch.var_mean
package: torch
ret_type: (Tensor, Tensor)
target: var_mean
title: torch.var_mean
version: 1.9.0
