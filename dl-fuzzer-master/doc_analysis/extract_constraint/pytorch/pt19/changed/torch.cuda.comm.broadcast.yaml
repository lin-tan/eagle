constraints:
  devices:
    default: None
    descp: an iterable of GPU devices, among which to broadcast.
    doc_dtype:
    - Iterable[torch.device
    - str
    - int]
    dtype:
    - string
    structure:
    - list
  out:
    default: None
    descp: the GPU tensors to store output results.
    doc_dtype:
    - Sequence[Tensor]
    - keyword-only
  tensor:
    descp: tensor to broadcast. Can be on CPU or GPU.
    doc_dtype:
    - Tensor
    tensor_t:
    - torch.tensor
inputs:
  keyword_only:
  - out
  optional:
  - devices
  - out
  required:
  - tensor
link: https://pytorch.org/docs/1.7.0/cuda.html#torch.cuda.comm.broadcast
package: torch
target: broadcast
title: torch.cuda.comm.broadcast
version: 1.9.0
