aliases:
- tf.compat.v1.keras.activations.hard_sigmoid
constraints:
  x:
    descp: Input tensor.
    tensor_t:
    - tf.tensor
inputs:
  optional: []
  required:
  - x
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/keras/activations/hard_sigmoid
outputs:
- 'The hard sigmoid activation, defined as: if x < -2.5: return 0 if x > 2.5: return
  1 if -2.5 <= x <= 2.5: return 0.2 * x + 0.5'
package: tensorflow
target: hard_sigmoid
title: tf.keras.activations.hard_sigmoid
version: 2.3.0
