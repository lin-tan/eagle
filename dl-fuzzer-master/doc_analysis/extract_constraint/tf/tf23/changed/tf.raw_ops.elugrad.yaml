constraints:
  gradients:
    descp: 'A Tensor. Must be one of the following types: half, bfloat16, float32,
      float64. The backpropagated gradients to the corresponding Elu operation.'
    tensor_t:
    - tf.tensor
  name:
    default: None
    descp: A name for the operation (optional).
    dtype:
    - tf.string
    ndim:
    - '0'
  outputs:
    descp: A Tensor. Must have the same type as gradients. The outputs of the corresponding
      Elu operation.
    dtype:
    - dtype:&gradients
    tensor_t:
    - tf.tensor
inputs:
  optional:
  - name
  required:
  - gradients
  - outputs
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/raw_ops/EluGrad
outputs:
- A Tensor. Has the same type as gradients.
package: tensorflow
target: EluGrad
title: tf.raw_ops.EluGrad
version: 2.3.0
