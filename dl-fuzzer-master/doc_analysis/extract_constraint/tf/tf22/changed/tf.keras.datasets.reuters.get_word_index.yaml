aliases:
- tf.compat.v1.keras.datasets.reuters.get_word_index
constraints:
  path:
    default: reuters_word_index.json
    descp: where to cache the data (relative to ~/.keras/dataset).
    dtype:
    - tf.string
inputs:
  optional:
  - path
  required: []
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/keras/datasets/reuters/get_word_index
outputs:
- The word index dictionary. Keys are word strings, values are their index.
package: tensorflow
target: get_word_index
title: tf.keras.datasets.reuters.get_word_index
version: 2.2.0
