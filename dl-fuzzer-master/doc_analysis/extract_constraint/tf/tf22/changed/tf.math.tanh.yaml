aliases:
- tf.nn.tanh
- tf.tanh
- tf.compat.v1.math.tanh
- tf.compat.v1.nn.tanh
- tf.compat.v1.tanh
constraints:
  name:
    default: None
    descp: A name for the operation (optional).
    dtype:
    - tf.string
    ndim:
    - '0'
  x:
    descp: 'A Tensor. Must be one of the following types: bfloat16, half, float32,
      float64, complex64, complex128.'
    tensor_t:
    - tf.tensor
inputs:
  optional:
  - name
  required:
  - x
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/math/tanh
outputs:
- A Tensor. Has the same type as x. If x is a SparseTensor, returns SparseTensor(x.indices,
  tf.math.tanh(x.values, ...), x.dense_shape)
package: tensorflow
target: tanh
title: tf.math.tanh
version: 2.2.0
