aliases:
- tf.compat.v1.keras.backend.concatenate
constraints:
  axis:
    default: '-1'
    descp: concatenation axis.
    dtype:
    - int
    ndim:
    - '0'
  tensors:
    descp: list of tensors to concatenate.
    structure:
    - list(tf.tensor)
    tensor_t:
    - tf.tensor
inputs:
  optional:
  - axis
  required:
  - tensors
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/keras/backend/concatenate
outputs:
- A tensor.
package: tensorflow
target: concatenate
title: tf.keras.backend.concatenate
version: 2.2.0
