aliases:
- tf.compat.v1.erf
- tf.compat.v1.math.erf
constraints:
  name:
    default: None
    descp: A name for the operation (optional).
    dtype:
    - tf.string
    ndim:
    - '0'
  x:
    descp: 'A Tensor. Must be one of the following types: bfloat16, half, float32,
      float64.'
    tensor_t:
    - tf.tensor
inputs:
  optional:
  - name
  required:
  - x
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/math/erf
outputs:
- A Tensor. Has the same type as x. If x is a SparseTensor, returns SparseTensor(x.indices,
  tf.math.erf(x.values, ...), x.dense_shape)
package: tensorflow
target: erf
title: tf.math.erf
version: 2.2.0
