constraints:
  padding:
    descp: the size of the padding. If is int, uses the same padding in all boundaries.
      If a 4-tuple, uses (padding _left , padding _right , padding _top , padding
      _bottom )
    doc_dtype:
    - int
    - tuple
inputs:
  optional: []
  required:
  - padding
link: https://pytorch.org/docs/stable/nn.html#torch.nn.ReplicationPad2d
package: torch
target: ReplicationPad2d
title: torch.nn.ReplicationPad2d
version: 1.5.0
