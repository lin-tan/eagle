constraints:
  parameters:
    descp: an iterator of Tensors that are the parameters of a model.
    doc_dtype:
    - Iterable[Tensor]
  vec:
    descp: a single vector represents the parameters of a model.
    doc_dtype:
    - Tensor
inputs:
  optional: []
  required:
  - vec
  - parameters
link: https://pytorch.org/docs/1.6.0/generated/torch.nn.utils.vector_to_parameters.html#torch.nn.utils.vector_to_parameters#torch.nn.utils.vector_to_parameters
package: torch
target: vector_to_parameters
title: torch.nn.utils.vector_to_parameters
version: 1.6.0
