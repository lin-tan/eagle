constraints:
  device:
    descp: selected device. This function is a no-op if this argument is negative.
    doc_dtype:
    - torch.device
    - int
    sig_dtype: Union[torch.device,str,int]
inputs:
  optional: []
  required:
  - device
link: https://pytorch.org/docs/1.6.0/cuda.html#torch.cuda.set_device
package: torch
ret_type: None
target: set_device
title: torch.cuda.set_device
version: 1.6.0
