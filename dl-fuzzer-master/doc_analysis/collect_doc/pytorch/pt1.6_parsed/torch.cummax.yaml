constraints:
  dim:
    descp: the dimension to do the operation over
    doc_dtype:
    - int
  input:
    descp: the input tensor.
    doc_dtype:
    - Tensor
  out:
    default: None
    descp: the result tuple of two output tensors (values, indices)
    doc_dtype:
    - tuple
inputs:
  optional:
  - out
  required:
  - input
  - dim
link: https://pytorch.org/docs/1.6.0/generated/torch.cummax.html#torch.cummax#torch.cummax
package: torch
ret_type: (Tensor, LongTensor)
target: cummax
title: torch.cummax
version: 1.6.0
