constraints:
  input:
    descp: 1-d int tensor
    doc_dtype:
    - Tensor
  minlength:
    default: '0'
    descp: optional, minimum number of bins. Should be non-negative.
    doc_dtype:
    - int
  weights:
    default: None
    descp: optional, weight for each value in the input tensor. Should be of same
      size as input tensor.
    doc_dtype:
    - Tensor
inputs:
  optional:
  - weights
  - minlength
  required:
  - input
link: https://pytorch.org/docs/1.6.0/generated/torch.bincount.html#torch.bincount#torch.bincount
package: torch
ret_type: Tensor
target: bincount
title: torch.bincount
version: 1.6.0
