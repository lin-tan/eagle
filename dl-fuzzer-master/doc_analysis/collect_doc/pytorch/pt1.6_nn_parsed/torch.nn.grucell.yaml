constraints:
  bias:
    default: 'True'
    descp: 'If `False`, then the layer does not use bias weights b_ih and b_hh. Default:
      `True`'
    sig_dtype: bool
  hidden_size:
    descp: The number of features in the hidden state h
    sig_dtype: int
  input_size:
    descp: The number of expected features in the input x
    sig_dtype: int
inputs:
  optional:
  - bias
  required:
  - input_size
  - hidden_size
link: https://pytorch.org/docs/1.6.0/generated/torch.nn.GRUCell.html#torch.nn.GRUCell#torch.nn.GRUCell
package: torch
target: GRUCell
title: torch.nn.GRUCell
version: 1.6.0
