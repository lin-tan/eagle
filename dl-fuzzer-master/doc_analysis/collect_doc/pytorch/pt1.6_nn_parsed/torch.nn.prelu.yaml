constraints:
  init:
    default: '0.25'
    descp: 'the initial value of a . Default: 0.25'
    doc_dtype:
    - float
    sig_dtype: float
  num_parameters:
    default: '1'
    descp: 'number of a  to learn. Although it takes an int as input, there is only
      two values are legitimate: 1, or the number of channels at input. Default: 1'
    doc_dtype:
    - int
    sig_dtype: int
inputs:
  optional:
  - num_parameters
  - init
  required: []
link: https://pytorch.org/docs/1.6.0/generated/torch.nn.PReLU.html#torch.nn.PReLU#torch.nn.PReLU
package: torch
target: PReLU
title: torch.nn.PReLU
version: 1.6.0
