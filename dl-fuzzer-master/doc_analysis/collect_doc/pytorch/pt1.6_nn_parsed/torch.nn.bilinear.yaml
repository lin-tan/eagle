constraints:
  bias:
    default: 'True'
    descp: 'If set to False, the layer will not learn an additive bias. Default: `True`'
    sig_dtype: bool
  in1_features:
    descp: size of each first input sample
    sig_dtype: int
  in2_features:
    descp: size of each second input sample
    sig_dtype: int
  out_features:
    descp: size of each output sample
    sig_dtype: int
inputs:
  optional:
  - bias
  required:
  - in1_features
  - in2_features
  - out_features
link: https://pytorch.org/docs/1.6.0/generated/torch.nn.Bilinear.html#torch.nn.Bilinear#torch.nn.Bilinear
package: torch
target: Bilinear
title: torch.nn.Bilinear
version: 1.6.0
