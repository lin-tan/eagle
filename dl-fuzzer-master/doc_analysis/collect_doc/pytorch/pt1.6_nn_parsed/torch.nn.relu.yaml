constraints:
  inplace:
    default: 'False'
    descp: 'can optionally do the operation in-place. Default: `False`'
    sig_dtype: bool
inputs:
  optional:
  - inplace
  required: []
link: https://pytorch.org/docs/1.6.0/generated/torch.nn.ReLU.html#torch.nn.ReLU#torch.nn.ReLU
package: torch
target: ReLU
title: torch.nn.ReLU
version: 1.6.0
