constraints:
  eps:
    default: 1e-06
    descp: 'Small value to avoid division by zero. Default: 1e-6'
    doc_dtype:
    - float
    sig_dtype: float
  keepdim:
    default: 'False'
    descp: 'Determines whether or not to keep the vector dimension. Default: False'
    doc_dtype:
    - bool
    sig_dtype: bool
  p:
    default: '2.0'
    descp: 'the norm degree. Default: 2'
    doc_dtype:
    - real
    sig_dtype: float
inputs:
  optional:
  - p
  - eps
  - keepdim
  required: []
link: https://pytorch.org/docs/1.6.0/generated/torch.nn.PairwiseDistance.html#torch.nn.PairwiseDistance#torch.nn.PairwiseDistance
package: torch
target: PairwiseDistance
title: torch.nn.PairwiseDistance
version: 1.6.0
