constraints:
  inplace:
    default: 'False'
    descp: 'can optionally do the operation in-place. Default: `False`'
    sig_dtype: bool
  threshold:
    descp: The value to threshold at
    sig_dtype: float
  value:
    descp: The value to replace with
    sig_dtype: float
inputs:
  optional:
  - inplace
  required:
  - threshold
  - value
link: https://pytorch.org/docs/1.6.0/generated/torch.nn.Threshold.html#torch.nn.Threshold#torch.nn.Threshold
package: torch
target: Threshold
title: torch.nn.Threshold
version: 1.6.0
