constraints:
  dim:
    default: None
    descp: 'a dimension or a list of dimensions to reduce. Default: reduce over all
      dims.'
    doc_dtype:
    - int
    - tuple of python:ints
    sig_dtype: Optional[Tuple[int]]
  dtype:
    default: None
    descp: 'the desired data type of returned Tensor. Default: dtype of `input`.'
    doc_dtype:
    - torch.dtype
    sig_dtype: Optional[int]
  input:
    descp: the input SparseTensor
    doc_dtype:
    - Tensor
    sig_dtype: torch.Tensor
inputs:
  optional:
  - dim
  - dtype
  required:
  - input
link: https://pytorch.org/docs/1.7.0/sparse.html#torch.sparse.sum
package: torch
ret_type: torch.Tensor
target: sum
title: torch.sparse.sum
version: 1.7.0
