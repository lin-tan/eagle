constraints:
  device:
    default: None
    descp: selected device. Returns the currently selected `Stream` for the current
      device, given by `current_device()`, if `device` is `None` (default).
    doc_dtype:
    - torch.device
    - int
    sig_dtype: Union[torch.device,str,int,None]
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/1.7.0/cuda.html#torch.cuda.current_stream
package: torch
ret_type: torch.cuda.streams.Stream
target: current_stream
title: torch.cuda.current_stream
version: 1.7.0
