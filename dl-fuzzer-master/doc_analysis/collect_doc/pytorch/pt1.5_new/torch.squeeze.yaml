constraints:
  dim:
    default: None
    descp: if given, the input will be squeezed only in this dimension
    doc_dtype: int, optional
  input:
    descp: the input tensor.
    doc_dtype: Tensor
  out:
    default: None
    descp: the output tensor.
    doc_dtype: Tensor, optional
inputs:
  optional:
  - dim
  - out
  required:
  - input
link: https://pytorch.org/docs/stable/torch.html#torch.squeeze
package: torch
ret_type: Tensor
target: squeeze
title: torch.squeeze
version: 1.5.0
