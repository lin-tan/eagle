constraints:
  input:
    descp: the input tensor.
    doc_dtype:
    - Tensor
  mask:
    descp: the tensor containing the binary mask to index with
    doc_dtype:
    - ByteTensor
  out:
    default: None
    descp: the output tensor.
    doc_dtype:
    - Tensor
inputs:
  optional:
  - out
  required:
  - input
  - mask
link: https://pytorch.org/docs/1.4.0/torch.html#torch.masked_select
package: torch
ret_type: Tensor
target: masked_select
title: torch.masked_select
version: 1.4.0
