constraints:
  a:
    descp: A Tensor.
  conjugate:
    default: 'False'
    descp: Optional bool. Setting it to True is mathematically equivalent to tf.math.conj(tf.transpose(input)).
  name:
    default: transpose
    descp: A name for the operation (optional).
  perm:
    default: None
    descp: A permutation of the dimensions of a. This should be a vector.
inputs:
  optional:
  - perm
  - conjugate
  - name
  required:
  - a
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/transpose
outputs:
- A transposed Tensor.
package: tensorflow
target: transpose
title: tf.transpose
version: 2.3.0
