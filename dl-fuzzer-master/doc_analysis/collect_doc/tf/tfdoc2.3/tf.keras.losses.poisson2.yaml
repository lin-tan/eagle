aliases:
- tf.keras.metrics.poisson
- tf.losses.poisson
- tf.metrics.poisson
- tf.compat.v1.keras.losses.poisson
- tf.compat.v1.keras.metrics.poisson
constraints:
  y_pred:
    descp: The predicted values. shape = [batch_size, d0, .. dN].
  y_true:
    descp: Ground truth values. shape = [batch_size, d0, .. dN].
exceptions:
- InvalidArgumentError: If y_true and y_pred have incompatible shapes.
inputs:
  optional: []
  required:
  - y_true
  - y_pred
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/keras/losses/poisson
outputs:
- Poisson loss value. shape = [batch_size, d0, .. dN-1].
package: tensorflow
target: poisson
title: tf.keras.losses.poisson
version: 2.3.0
