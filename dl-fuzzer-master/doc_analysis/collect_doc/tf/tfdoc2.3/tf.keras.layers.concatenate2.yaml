aliases:
- tf.compat.v1.keras.layers.concatenate
constraints:
  '**kwargs':
    default: null
    descp: Standard layer keyword arguments.
  axis:
    default: '-1'
    descp: Concatenation axis.
  inputs:
    descp: A list of input tensors (at least 2).
inputs:
  optional:
  - axis
  - '**kwargs'
  required:
  - inputs
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/keras/layers/concatenate
outputs:
- A tensor, the concatenation of the inputs alongside axis axis.
package: tensorflow
target: concatenate
title: tf.keras.layers.concatenate
version: 2.3.0
