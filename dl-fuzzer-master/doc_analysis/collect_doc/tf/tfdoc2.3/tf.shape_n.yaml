aliases:
- tf.compat.v1.shape_n
constraints:
  input:
    descp: A list of at least 1 Tensor object with the same type.
  name:
    default: None
    descp: A name for the operation (optional).
  out_type:
    default: tf.dtypes.int32
    descp: The specified output type of the operation (int32 or int64). Defaults to
      tf.int32(optional).
inputs:
  optional:
  - out_type
  - name
  required:
  - input
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/shape_n
outputs:
- A list with the same length as input of Tensor objects with type out_type.
package: tensorflow
target: shape_n
title: tf.shape_n
version: 2.3.0
