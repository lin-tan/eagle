constraints:
  images:
    descp: 'A Tensor. Must be one of the following types: half, bfloat16, float32,
      float64. 1-D or higher rank. RGB data to convert. Last dimension must be size
      3.'
  name:
    default: None
    descp: A name for the operation (optional).
inputs:
  optional:
  - name
  required:
  - images
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/raw_ops/RGBToHSV
outputs:
- A Tensor. Has the same type as images.
package: tensorflow
target: RGBToHSV
title: tf.raw_ops.RGBToHSV
version: 2.3.0
