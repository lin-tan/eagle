constraints:
  inputs:
    descp: A list of at least 1 mutable Tensor objects with the same type. The input
      tensors, exactly one of which will become available.
  name:
    default: None
    descp: A name for the operation (optional).
inputs:
  optional:
  - name
  required:
  - inputs
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/raw_ops/RefMerge
outputs:
- A tuple of Tensor objects (output, value_index).
- output: A mutable Tensor. Has the same type as inputs.
- value_index: A Tensor of type int32.
package: tensorflow
target: RefMerge
title: tf.raw_ops.RefMerge
version: 2.3.0
