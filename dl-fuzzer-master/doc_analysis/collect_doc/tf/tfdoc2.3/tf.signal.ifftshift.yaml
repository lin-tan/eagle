aliases:
- tf.compat.v1.signal.ifftshift
constraints:
  axes:
    default: None
    descp: int or shape tuple Axes over which to calculate. Defaults to None, which
      shifts all axes.
  name:
    default: None
    descp: An optional name for the operation.
  x:
    descp: Tensor, input tensor.
inputs:
  optional:
  - axes
  - name
  required:
  - x
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/signal/ifftshift
outputs:
- A Tensor, The shifted tensor.
package: tensorflow
target: ifftshift
title: tf.signal.ifftshift
version: 2.3.0
