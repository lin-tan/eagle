aliases:
- tf.compat.v1.data.experimental.cardinality
constraints:
  dataset:
    descp: A `tf.data.Dataset` for which to determine cardinality.
inputs:
  optional: []
  required:
  - dataset
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/data/experimental/cardinality
outputs: A scalar `tf.int64` `Tensor` representing the cardinality of `dataset`. If
  the cardinality is infinite or unknown, the operation returns the named constant
  `INFINITE_CARDINALITY` and `UNKNOWN_CARDINALITY` respectively.
package: tensorflow
target: cardinality
title: tf.data.experimental.cardinality
version: 2.1.0
