aliases:
- tf.reduce_max
constraints:
  axis:
    default: None
    descp: The dimensions to reduce. If `None` (the default), reduces all dimensions.
      Must be in the range `[-rank(input_tensor), rank(input_tensor))`.
  input_tensor:
    descp: The tensor to reduce. Should have real numeric type.
  keepdims:
    default: 'False'
    descp: If true, retains reduced dimensions with length 1.
  name:
    default: None
    descp: A name for the operation (optional).
inputs:
  optional:
  - axis
  - keepdims
  - name
  required:
  - input_tensor
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/math/reduce_max
outputs: The reduced tensor.
package: tensorflow
target: reduce_max
title: tf.math.reduce_max
version: 2.1.0
