aliases:
- tf.minimum
- tf.compat.v1.math.minimum
- tf.compat.v1.minimum
constraints:
  name:
    default: None
    descp: A name for the operation (optional).
  x:
    descp: 'A `Tensor`. Must be one of the following types: `bfloat16`, `half`, `float32`,
      `float64`, `int32`, `int64`.'
  y:
    descp: A `Tensor`. Must have the same type as `x`.
inputs:
  optional:
  - name
  required:
  - x
  - y
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/math/minimum
outputs: A `Tensor`. Has the same type as `x`.
package: tensorflow
target: minimum
title: tf.math.minimum
version: 2.1.0
