constraints:
  name:
    default: None
    descp: A name for the operation (optional).
  output_shapes:
    descp: A list of shapes (each a tf.TensorShape or list of ints) that has length
      >= 1.
  output_types:
    descp: A list of tf.DTypes that has length >= 1.
inputs:
  optional:
  - name
  required:
  - output_types
  - output_shapes
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/raw_ops/AnonymousIterator
outputs:
- A Tensor of type resource.
package: tensorflow
target: AnonymousIterator
title: tf.raw_ops.AnonymousIterator
version: 2.2.0
