constraints:
  inputs:
    descp: A list of at least 1 Tensor objects with type string. A list of string
      tensors. The tensors must all have the same shape, or be scalars. Scalars may
      be mixed in; these will be broadcast to the shape of non-scalar inputs.
  name:
    default: None
    descp: A name for the operation (optional).
  separator:
    default: ''
    descp: An optional string. Defaults to "". string, an optional join separator.
inputs:
  optional:
  - separator
  - name
  required:
  - inputs
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/raw_ops/StringJoin
outputs:
- A Tensor of type string.
package: tensorflow
target: StringJoin
title: tf.raw_ops.StringJoin
version: 2.2.0
