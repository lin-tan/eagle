aliases:
- tf.keras.metrics.hinge
- tf.losses.hinge
- tf.metrics.hinge
- tf.compat.v1.keras.losses.hinge
- tf.compat.v1.keras.metrics.hinge
constraints:
  y_pred:
    descp: The predicted values. shape = [batch_size, d0, .. dN].
  y_true:
    descp: The ground truth values. y_true values are expected to be -1 or 1. If binary
      (0 or 1) labels are provided they will be converted to -1 or 1. shape = [batch_size,
      d0, .. dN].
inputs:
  optional: []
  required:
  - y_true
  - y_pred
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/keras/losses/hinge
outputs:
- Hinge loss values. shape = [batch_size, d0, .. dN-1].
package: tensorflow
target: hinge
title: tf.keras.losses.hinge
version: 2.2.0
