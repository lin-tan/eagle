aliases:
- tf.compat.v1.keras.layers.average
constraints:
  '**kwargs':
    default: null
    descp: Standard layer keyword arguments.
  inputs:
    descp: A list of input tensors (at least 2).
exceptions:
- ValueError: If there is a shape mismatch between the inputs and the shapes cannot
    be broadcasted to match.
inputs:
  optional:
  - '**kwargs'
  required:
  - inputs
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/keras/layers/average
outputs:
- A tensor, the average of the inputs.
package: tensorflow
target: average
title: tf.keras.layers.average
version: 2.2.0
