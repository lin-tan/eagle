constraints:
  container:
    default: ''
    descp: An optional string. Defaults to "". If non-empty, this reader is placed
      in the given container. Otherwise, a default container is used.
  name:
    default: None
    descp: A name for the operation (optional).
  shared_name:
    default: ''
    descp: An optional string. Defaults to "". If non-empty, this reader is named
      in the given bucket with this shared_name. Otherwise, the node name is used
      instead.
inputs:
  optional:
  - container
  - shared_name
  - name
  required: []
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/raw_ops/IdentityReader
outputs:
- A Tensor of type mutable string.
package: tensorflow
target: IdentityReader
title: tf.raw_ops.IdentityReader
version: 2.2.0
