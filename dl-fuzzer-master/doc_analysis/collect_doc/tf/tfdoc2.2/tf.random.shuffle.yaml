aliases:
- tf.compat.v1.random.shuffle
- tf.compat.v1.random_shuffle
constraints:
  name:
    default: None
    descp: A name for the operation (optional).
  seed:
    default: None
    descp: A Python integer. Used to create a random seed for the distribution. See
      tf.random.set_seed for behavior.
  value:
    descp: A Tensor to be shuffled.
inputs:
  optional:
  - seed
  - name
  required:
  - value
link: https://www.tensorflow.org/versions/r2.2/api_docs/python/tf/random/shuffle
outputs:
- A tensor of same shape and type as value, shuffled along its first dimension.
package: tensorflow
target: shuffle
title: tf.random.shuffle
version: 2.2.0
