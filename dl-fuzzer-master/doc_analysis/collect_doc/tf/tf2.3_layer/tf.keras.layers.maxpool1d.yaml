aliases:
- tf.keras.layers.MaxPooling1D
- tf.compat.v1.keras.layers.MaxPool1D
- tf.compat.v1.keras.layers.MaxPooling1D
constraints:
  '**kwargs':
    descp: ''
  data_format:
    default: channels_last
    descp: A string, one of channels_last (default) or channels_first. The ordering
      of the dimensions in the inputs. channels_last corresponds to inputs with shape
      (batch, steps, features) while channels_first corresponds to inputs with shape
      (batch, features, steps).
  padding:
    default: valid
    descp: One of "valid" or "same" (case-insensitive). "valid" adds no padding. "same"
      adds padding such that if the stride is 1, the output shape is the same as the
      input shape.
  pool_size:
    default: '2'
    descp: Integer, size of the max pooling window.
  strides:
    default: None
    descp: Integer, or None. Specifies how much the pooling window moves for each
      pooling step. If None, it will default to pool_size.
inputs:
  optional:
  - pool_size
  - strides
  - padding
  - data_format
  required:
  - '**kwargs'
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/keras/layers/MaxPool1D
package: tensorflow
target: MaxPool1D
title: tf.keras.layers.MaxPool1D
version: 2.3.0
