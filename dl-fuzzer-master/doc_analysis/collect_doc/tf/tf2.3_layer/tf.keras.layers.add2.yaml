aliases:
- tf.compat.v1.keras.layers.add
constraints:
  '**kwargs':
    descp: Standard layer keyword arguments.
  inputs:
    descp: A list of input tensors (at least 2) with the same shape.
inputs:
  optional: []
  required:
  - inputs
  - '**kwargs'
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/keras/layers/add
outputs: A tensor as the sum of the inputs. It has the same shape as the inputs.
package: tensorflow
target: add
title: tf.keras.layers.add
version: 2.3.0
