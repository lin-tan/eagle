aliases:
- tf.compat.v1.keras.layers.InputLayer
constraints:
  '**kwargs':
    descp: ''
  batch_size:
    default: None
    descp: Optional input batch size (integer or None).
  dtype:
    default: None
    descp: Optional datatype of the input. When not provided, the Keras default float
      type will be used.
  input_shape:
    default: None
    descp: Shape tuple (not including the batch axis), or TensorShape instance (not
      including the batch axis).
  input_tensor:
    default: None
    descp: Optional tensor to use as layer input instead of creating a placeholder.
  name:
    default: None
    descp: Optional name of the layer (string).
  ragged:
    default: 'False'
    descp: Boolean, whether the placeholder created is meant to be ragged. In this
      case, values of 'None' in the 'shape' argument represent ragged dimensions.
      For more information about RaggedTensors, see this guide. Default to False.
  sparse:
    default: 'False'
    descp: Boolean, whether the placeholder created is meant to be sparse. Default
      to False.
inputs:
  optional:
  - input_shape
  - batch_size
  - dtype
  - input_tensor
  - sparse
  - name
  - ragged
  required:
  - '**kwargs'
link: https://www.tensorflow.org/versions/r2.3/api_docs/python/tf/keras/layers/InputLayer
package: tensorflow
target: InputLayer
title: tf.keras.layers.InputLayer
version: 2.3.0
