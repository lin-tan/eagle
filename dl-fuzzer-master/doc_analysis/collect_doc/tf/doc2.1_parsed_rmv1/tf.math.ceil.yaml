aliases:
- tf.compat.v1.ceil
- tf.compat.v1.math.ceil
constraints:
  name:
    default: None
    descp: A name for the operation (optional).
  x:
    descp: 'A `Tensor`. Must be one of the following types: `bfloat16`, `half`, `float32`,
      `float64`.'
inputs:
  optional:
  - name
  required:
  - x
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/math/ceil
outputs: A `Tensor`. Has the same type as `x`.
package: tensorflow
target: ceil
title: tf.math.ceil
version: 2.1.0
