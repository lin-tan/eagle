constraints:
  X: {descp: 'array-like, shape (n_samples, n_features) The data.'}
  axis: {default: '0', descp: 'int (0 by default) axis used to scale along. If 0,
      independently scale each feature, otherwise (if 1) scale each sample.'}
  copy: {default: 'True', descp: 'boolean, optional, default is True Set to False
      to perform inplace scaling and avoid a copy (if the input is already a numpy
      array).'}
inputs:
  optional: [axis, copy]
  required: [X]
link: ''
package: sklearn
target: maxabs_scale
title: sklearn.preprocessing.maxabs_scale
version: 0.24.2
