constraints:
  average_method: {default: arithmetic, descp: 'string, optional (default: ''arithmetic'')
      How to compute the normalizer in the denominator. Possible options are ''min'',
      ''geometric'', ''arithmetic'', and ''max''.'}
  labels_pred: {descp: 'int array-like of shape (n_samples,) A clustering of the data
      into disjoint subsets.'}
  labels_true: {descp: 'int array, shape = [n_samples] A clustering of the data into
      disjoint subsets.'}
inputs:
  optional: [average_method]
  required: [labels_true, labels_pred]
link: ''
package: sklearn
target: adjusted_mutual_info_score
title: sklearn.metrics.adjusted_mutual_info_score
version: 0.24.2
