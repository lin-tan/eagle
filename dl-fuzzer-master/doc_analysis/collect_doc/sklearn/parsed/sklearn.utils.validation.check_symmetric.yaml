constraints:
  array: {descp: 'nd-array or sparse matrix Input object to check / convert. Must
      be two-dimensional and square, otherwise a ValueError will be raised. tol :
      float Absolute tolerance for equivalence of arrays. Default = 1E-10. raise_warning
      : boolean (default=True) If True then raise a warning if conversion is required.
      raise_exception : boolean (default=False) If True then raise an exception if
      array is not symmetric.'}
  raise_exception: {default: 'False', descp: ''}
  raise_warning: {default: 'True', descp: ''}
  tol: {default: 1e-10, descp: ''}
inputs:
  optional: [tol, raise_warning, raise_exception]
  required: [array]
link: ''
package: sklearn
target: check_symmetric
title: sklearn.utils.validation.check_symmetric
version: 0.24.2
