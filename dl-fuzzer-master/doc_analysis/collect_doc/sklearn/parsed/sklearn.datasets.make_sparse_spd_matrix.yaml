constraints:
  alpha: {default: '0.95', descp: 'float between 0 and 1, optional (default=0.95)
      The probability that a coefficient is zero (see notes). Larger values enforce
      more sparsity.'}
  dim: {default: '1', descp: 'integer, optional (default=1) The size of the random
      matrix to generate.'}
  largest_coef: {default: '0.9', descp: 'float between 0 and 1, optional (default=0.9)
      The value of the largest coefficient.'}
  norm_diag: {default: 'False', descp: 'boolean, optional (default=False) Whether
      to normalize the output matrix to make the leading diagonal elements all 1'}
  random_state: {default: None, descp: 'int, RandomState instance or None (default)
      Determines random number generation for dataset creation. Pass an int for reproducible
      output across multiple function calls. See :term:`Glossary <random_state>`.'}
  smallest_coef: {default: '0.1', descp: 'float between 0 and 1, optional (default=0.1)
      The value of the smallest coefficient.'}
inputs:
  optional: [dim, alpha, norm_diag, smallest_coef, largest_coef, random_state]
  required: []
link: ''
package: sklearn
target: make_sparse_spd_matrix
title: sklearn.datasets.make_sparse_spd_matrix
version: 0.24.2
