constraints:
  alpha:
    default: '1.0'
    descp: 'the  alpha  value for the CELU formulation. Default: 1.0'
    dtype:
    - torch.float32
    ndim:
    - '0'
  inplace:
    default: 'False'
    descp: 'can optionally do the operation in-place. Default: `False`'
    dtype:
    - torch.bool
    ndim:
    - '0'
inputs:
  optional:
  - alpha
  - inplace
  required: []
link: https://pytorch.org/docs/stable/nn.html#torch.nn.CELU
package: torch
target: CELU
title: torch.nn.CELU
version: 1.5.0
layer_constructor: true
check_nan: true
