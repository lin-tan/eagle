constraints:
  mask:
    descp: binary mask to be applied to the parameter.
    doc_dtype: Tensor
    normalized_descp:
    - binary mask to be applied to the parameter
    normalized_docdtype: ONE_WORD D_STRUCTURE
  module:
    descp: module containing the tensor to prune
    doc_dtype: nn.Module
    normalized_descp:
    - module containing the D_STRUCTURE to prune
    normalized_docdtype: nn Module
  name:
    descp: parameter name within `module` on which pruning will act.
    doc_dtype: str
    normalized_descp:
    - parameter name within PARAM on which pruning will act
    normalized_docdtype: ONE_WORD D_TYPE
inputs:
  optional: []
  required:
  - module
  - name
  - mask
link: https://pytorch.org/docs/stable/nn.html#torch.nn.utils.prune.custom_from_mask
package: torch
target: custom_from_mask
title: torch.nn.utils.prune.custom_from_mask
version: 1.5.0
