constraints:
  kernel_size:
    descp: Size of the max pooling window.
    doc_dtype: int or tuple
    normalized_descp:
    - Size of the max pooling window
    normalized_docdtype: D_TYPE or D_STRUCTURE
  padding:
    default: '0'
    descp: Padding that was added to the input
    doc_dtype: int or tuple
    normalized_default: DEFAULT CONSTANT_NUM
    normalized_descp:
    - Padding that was added to the input
    normalized_docdtype: D_TYPE or D_STRUCTURE
  stride:
    default: None
    descp: Stride of the max pooling window. It is set to `kernel_size` by default.
    doc_dtype: int or tuple
    normalized_default: DEFAULT None
    normalized_descp:
    - Stride of the max pooling window
    - It is set to PARAM by default
    normalized_docdtype: D_TYPE or D_STRUCTURE
inputs:
  optional:
  - stride
  - padding
  required:
  - kernel_size
link: https://pytorch.org/docs/stable/nn.html#torch.nn.MaxUnpool1d
package: torch
target: MaxUnpool1d
title: torch.nn.MaxUnpool1d
version: 1.5.0
