constraints:
  input:
    descp: the PyTorch tensor to test
    doc_dtype: Tensor
    normalized_descp:
    - the PyTorch D_STRUCTURE to test
    normalized_docdtype: ONE_WORD D_STRUCTURE
inputs:
  optional: []
  required:
  - input
link: https://pytorch.org/docs/stable/torch.html#torch.is_floating_point
package: torch
ret_type: (bool)
target: is_floating_point
title: torch.is_floating_point
version: 1.5.0
