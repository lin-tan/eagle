constraints:
  _stacklevel:
    default: '3'
    descp: ''
    normalized_default: DEFAULT CONSTANT_NUM
    normalized_descp: []
  dim:
    default: None
    descp: A dimension along which softmax will be computed.
    doc_dtype: int
    normalized_default: DEFAULT None
    normalized_descp:
    - A dimension along which softmax will be computed
    normalized_docdtype: ONE_WORD D_TYPE
  dtype:
    default: None
    descp: 'the desired data type of returned tensor. If specified, the input tensor
      is casted to `dtype` before the operation is performed. This is useful for preventing
      data type overflows. Default: None.'
    doc_dtype: '`torch.dtype`, optional'
    normalized_default: DEFAULT None
    normalized_descp:
    - the desired data type of returned D_STRUCTURE
    - If specified, the PARAM D_STRUCTURE is casted to QSTR before the operation is
      performed
    - This is useful for preventing data type overflows
    - Default None
    normalized_docdtype: D_TYPE, optional
  input:
    descp: input
    doc_dtype: Tensor
    normalized_descp:
    - ONE_WORD input
    normalized_docdtype: ONE_WORD D_STRUCTURE
inputs:
  optional:
  - dim
  - _stacklevel
  - dtype
  required:
  - input
link: https://pytorch.org/docs/stable/nn.functional.html#torch.nn.functional.softmax
package: torch
target: softmax
title: torch.nn.functional.softmax
version: 1.5.0
