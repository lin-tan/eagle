constraints:
  inplace:
    default: 'False'
    descp: carry out model transformations in-place, the original module is mutated
    normalized_default: DEFAULT CONSTANT_BOOL
    normalized_descp:
    - carry out PARAM transformations in place, the original module is mutated
  model:
    descp: input model to be modified in-place
    normalized_descp:
    - input model to be modified in place
inputs:
  optional:
  - inplace
  required:
  - model
link: https://pytorch.org/docs/stable/quantization.html#torch.quantization.prepare
package: torch
target: prepare
title: torch.quantization.prepare
version: 1.5.0
