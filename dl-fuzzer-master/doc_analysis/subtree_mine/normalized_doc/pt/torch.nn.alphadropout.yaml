constraints:
  inplace:
    default: 'False'
    descp: If set to `True`, will do this operation in-place
    doc_dtype: bool, optional
    normalized_default: DEFAULT CONSTANT_BOOL
    normalized_descp:
    - If set to CONSTANT_BOOL, will do this operation in place
    normalized_docdtype: D_TYPE, optional
  p:
    default: '0.5'
    descp: 'probability of an element to be dropped. Default: 0.5'
    doc_dtype: float
    normalized_default: DEFAULT CONSTANT_FLOAT
    normalized_descp:
    - probability of an element to be dropped
    - Default CONSTANT_FLOAT
    normalized_docdtype: ONE_WORD D_TYPE
inputs:
  optional:
  - p
  - inplace
  required: []
link: https://pytorch.org/docs/stable/nn.html#torch.nn.AlphaDropout
package: torch
target: AlphaDropout
title: torch.nn.AlphaDropout
version: 1.5.0
