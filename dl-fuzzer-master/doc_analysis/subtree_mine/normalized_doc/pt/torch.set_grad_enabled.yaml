constraints:
  mode:
    descp: Flag whether to enable grad (`True`), or disable (`False`). This can be
      used to conditionally enable gradients.
    doc_dtype: bool
    normalized_descp:
    - Flag whether to enable grad CONSTANT_BOOL , or disable CONSTANT_BOOL
    - This can be used to conditionally enable gradients
    normalized_docdtype: ONE_WORD D_TYPE
inputs:
  optional: []
  required:
  - mode
link: https://pytorch.org/docs/stable/torch.html#torch.set_grad_enabled
package: torch
target: set_grad_enabled
title: torch.set_grad_enabled
version: 1.5.0
