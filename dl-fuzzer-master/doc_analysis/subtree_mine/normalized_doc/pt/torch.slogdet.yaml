constraints:
  input:
    descp: the input tensor of size `(*, n, n)` where `*` is zero or more batch dimensions.
    doc_dtype: Tensor
    normalized_descp:
    - the input D_STRUCTURE of size BSTR where is zero or more batch dimensions
    normalized_docdtype: ONE_WORD D_STRUCTURE
inputs:
  optional: []
  required:
  - input
link: https://pytorch.org/docs/stable/torch.html#torch.slogdet
package: torch
ret_type: (Tensor, Tensor)
target: slogdet
title: torch.slogdet
version: 1.5.0
