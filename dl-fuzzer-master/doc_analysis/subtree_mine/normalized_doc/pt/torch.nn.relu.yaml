constraints:
  inplace:
    default: 'False'
    descp: 'can optionally do the operation in-place. Default: `False`'
    normalized_default: DEFAULT CONSTANT_BOOL
    normalized_descp:
    - can optionally do the operation in place
    - Default CONSTANT_BOOL
inputs:
  optional:
  - inplace
  required: []
link: https://pytorch.org/docs/stable/nn.html#torch.nn.ReLU
package: torch
target: ReLU
title: torch.nn.ReLU
version: 1.5.0
