constraints:
  mode:
    descp: Controls whether to enable flush denormal mode or not
    doc_dtype: bool
    normalized_descp:
    - Controls whether to enable flush denormal mode or not
    normalized_docdtype: ONE_WORD D_TYPE
inputs:
  optional: []
  required:
  - mode
link: https://pytorch.org/docs/stable/torch.html#torch.set_flush_denormal
package: torch
ret_type: bool
target: set_flush_denormal
title: torch.set_flush_denormal
version: 1.5.0
