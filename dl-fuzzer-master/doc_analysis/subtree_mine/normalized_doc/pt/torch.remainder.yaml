constraints:
  input:
    descp: the dividend
    doc_dtype: Tensor
    normalized_descp:
    - the dividend
    normalized_docdtype: ONE_WORD D_STRUCTURE
  other:
    descp: the divisor that may be either a number or a Tensor of the same shape as
      the dividend
    doc_dtype: Tensor or float
    normalized_descp:
    - the divisor that may be either a number or a D_STRUCTURE of the same shape as
      the dividend
    normalized_docdtype: D_STRUCTURE or D_TYPE
  out:
    default: None
    descp: the output tensor.
    doc_dtype: Tensor, optional
    normalized_default: DEFAULT None
    normalized_descp:
    - the output D_STRUCTURE
    normalized_docdtype: D_STRUCTURE, optional
inputs:
  optional:
  - out
  required:
  - input
  - other
link: https://pytorch.org/docs/stable/torch.html#torch.remainder
package: torch
ret_type: Tensor
target: remainder
title: torch.remainder
version: 1.5.0
