constraints:
  input:
    descp: the tensor to compute the multivariate log-gamma function
    doc_dtype: Tensor
    normalized_descp:
    - the D_STRUCTURE to compute the multivariate log gamma function
    normalized_docdtype: ONE_WORD D_STRUCTURE
  p:
    descp: the number of dimensions
    doc_dtype: int
    normalized_descp:
    - the number of dimensions
    normalized_docdtype: ONE_WORD D_TYPE
inputs:
  optional: []
  required:
  - input
  - p
link: https://pytorch.org/docs/stable/torch.html#torch.mvlgamma
package: torch
ret_type: Tensor
target: mvlgamma
title: torch.mvlgamma
version: 1.5.0
