constraints:
  device:
    default: None
    descp: selected device. Returns the default `Stream` for the current device, given
      by `current_device()`, if `device` is `None` (default).
    doc_dtype: torch.device or int, optional
    normalized_default: DEFAULT None
    normalized_descp:
    - selected device
    - Returns the default QSTR for the current device, given by current_device , if
      QSTR is QSTR BSTR
    normalized_docdtype: torch device or D_TYPE, optional
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/stable/cuda.html#torch.cuda.default_stream
package: torch
target: default_stream
title: torch.cuda.default_stream
version: 1.5.0
