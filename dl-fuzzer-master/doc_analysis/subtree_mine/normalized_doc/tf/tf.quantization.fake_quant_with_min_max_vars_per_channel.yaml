aliases:
- tf.compat.v1.fake_quant_with_min_max_vars_per_channel
- tf.compat.v1.quantization.fake_quant_with_min_max_vars_per_channel
constraints:
  inputs:
    descp: A `Tensor` of type `float32`.
    normalized_descp:
    - A D_STRUCTURE of type D_TYPE
  max:
    descp: A `Tensor` of type `float32`.
    normalized_descp:
    - A D_STRUCTURE of type D_TYPE
  min:
    descp: A `Tensor` of type `float32`.
    normalized_descp:
    - A D_STRUCTURE of type D_TYPE
  name:
    default: None
    descp: A name for the operation (optional).
    normalized_default: DEFAULT None
    normalized_descp:
    - A name for the operation BSTR
  narrow_range:
    default: 'False'
    descp: An optional `bool`. Defaults to `False`.
    normalized_default: DEFAULT CONSTANT_BOOL
    normalized_descp:
    - An optional D_TYPE
    - Defaults to CONSTANT_BOOL
  num_bits:
    default: '8'
    descp: An optional `int`. Defaults to `8`.
    normalized_default: DEFAULT CONSTANT_NUM
    normalized_descp:
    - An optional D_TYPE
    - Defaults to CONSTANT_NUM
inputs:
  optional:
  - num_bits
  - narrow_range
  - name
  required:
  - inputs
  - min
  - max
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/quantization/fake_quant_with_min_max_vars_per_channel
outputs: A `Tensor` of type `float32`.
package: tensorflow
target: fake_quant_with_min_max_vars_per_channel
title: tf.quantization.fake_quant_with_min_max_vars_per_channel
version: 2.1.0
