aliases:
- tf.losses.categorical_hinge
- tf.compat.v1.keras.losses.categorical_hinge
constraints:
  y_pred:
    descp: The predicted values.
    normalized_descp:
    - The predicted values
  y_true:
    descp: The ground truth values. `y_true` values are expected to be -1 or 1. If
      binary (0 or 1) labels are provided they will be converted to -1 or 1.
    normalized_descp:
    - The ground truth values
    - QSTR values are expected to be CONSTANT_NUM
    - If binary BSTR labels are provided they will be converted to CONSTANT_NUM
inputs:
  optional: []
  required:
  - y_true
  - y_pred
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/losses/categorical_hinge
outputs: A tensor.
package: tensorflow
target: categorical_hinge
title: tf.keras.losses.categorical_hinge
version: 2.1.0
