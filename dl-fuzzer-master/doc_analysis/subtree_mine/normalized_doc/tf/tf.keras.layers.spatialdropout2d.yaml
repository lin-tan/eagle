aliases:
- tf.compat.v1.keras.layers.SpatialDropout2D
constraints:
  '**kwargs':
    default: null
    descp: ''
    normalized_descp: []
  data_format:
    default: None
    descp: '''channels_first'' or ''channels_last''. In ''channels_first'' mode, the
      channels dimension (the depth) is at index 1, in ''channels_last'' mode is it
      at index 3. It defaults to the `image_data_format` value found in your Keras
      config file at `~/.keras/keras.json`. If you never set it, then it will be "channels_last".'
    normalized_default: DEFAULT None
    normalized_descp:
    - ONE_WORD QSTR
    - In QSTR mode, the channels dimension BSTR is at index CONSTANT_NUM, in QSTR
      mode is it at index CONSTANT_NUM
    - It defaults to the QSTR value found in your Keras config file at keras keras
      json
    - If you never set it, then it will be QSTR
  rate:
    descp: Float between 0 and 1. Fraction of the input units to drop.
    normalized_descp:
    - D_TYPE between CONSTANT_NUM
    - Fraction of the input units to drop
inputs:
  optional:
  - data_format
  - '**kwargs'
  required:
  - rate
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/layers/SpatialDropout2D
package: tensorflow
target: SpatialDropout2D
title: tf.keras.layers.SpatialDropout2D
version: 2.1.0
