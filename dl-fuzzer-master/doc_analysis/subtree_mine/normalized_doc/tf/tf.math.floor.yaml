aliases:
- tf.floor
- tf.compat.v1.floor
- tf.compat.v1.math.floor
constraints:
  name:
    default: None
    descp: A name for the operation (optional).
    normalized_default: DEFAULT None
    normalized_descp:
    - A name for the operation BSTR
  x:
    descp: 'A `Tensor`. Must be one of the following types: `bfloat16`, `half`, `float32`,
      `float64`.'
    normalized_descp:
    - A D_STRUCTURE
    - Must be one of the following types D_TYPE
inputs:
  optional:
  - name
  required:
  - x
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/math/floor
outputs: A `Tensor`. Has the same type as `x`.
package: tensorflow
target: floor
title: tf.math.floor
version: 2.1.0
