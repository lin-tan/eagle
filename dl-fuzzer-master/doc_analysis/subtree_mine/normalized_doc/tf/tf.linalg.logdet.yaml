aliases:
- tf.compat.v1.linalg.logdet
constraints:
  matrix:
    descp: A `Tensor`. Must be `float16`, `float32`, `float64`, `complex64`, or `complex128`
      with shape `[..., M, M]`.
    normalized_descp:
    - A D_STRUCTURE
    - Must be D_TYPE with shape BSTR
  name:
    default: None
    descp: A name to give this `Op`.  Defaults to `logdet`.
    normalized_default: DEFAULT None
    normalized_descp:
    - A name to give this QSTR
    - Defaults to QSTR
inputs:
  optional:
  - name
  required:
  - matrix
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/linalg/logdet
outputs: The natural log of the determinant of `matrix`.
package: tensorflow
target: logdet
title: tf.linalg.logdet
version: 2.1.0
