constraints:
  return_X_y:
    default: 'False'
    descp: boolean, default=False. If True, returns ``(data, target)`` instead of
      a Bunch object. See below for more information about the `data` and `target`
      object.
    dtype:
    - boolean
    ndim:
    - '0'
    - '1'
inputs:
  optional:
  - return_X_y
  required: []
link: ''
package: sklearn
target: load_wine
title: sklearn.datasets.load_wine
version: 0.24.2
