constraints:
  sample_weight:
    default: None
    descp: array-like, shape (n_samples,), optional Sample weights.
    structure:
    - ndarray
  y_pred:
    descp: array-like of shape (n_samples,) Estimated target values. Requires y_pred
      > 0.
    ndim:
    - '2'
    shape:
    - '[n_samples,]'
    structure:
    - ndarray
  y_true:
    descp: array-like of shape (n_samples,) Ground truth (correct) target values.
      Requires y_true >= 0.
    structure:
    - ndarray
inputs:
  optional:
  - sample_weight
  required:
  - y_true
  - y_pred
link: ''
package: sklearn
target: mean_poisson_deviance
title: sklearn.metrics.mean_poisson_deviance
version: 0.24.2
