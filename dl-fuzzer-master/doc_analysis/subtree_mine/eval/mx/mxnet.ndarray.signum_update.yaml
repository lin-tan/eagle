constraints:
  clip_gradient:
    default: _Null
    descp: Clip gradient to the range of [-clip_gradient, clip_gradient] If clip_gradient
      <= 0, gradient clipping is turned off. grad = max(min(grad, clip_gradient),
      -clip_gradient).
    doc_dtype:
    - float
    - optional
    - default=-1
    dtype:
    - float
    ndim:
    - '0'
  name:
    default: None
    descp: ''
title: mxnet.ndarray.signum_update
