aliases:
- tf.keras.layers.GlobalAvgPool2D
- tf.compat.v1.keras.layers.GlobalAveragePooling2D
- tf.compat.v1.keras.layers.GlobalAvgPool2D
constraints:
  '**kwargs':
    default: null
    descp: ''
  data_format:
    default: None
    descp: A string, one of `channels_last` (default) or `channels_first`. The ordering
      of the dimensions in the inputs.`channels_last` corresponds to inputs with shape`(batch,
      height, width, channels)` while `channels_first`corresponds to inputs with shape`(batch,
      channels, height, width)`. It defaults to the `image_data_format` value found
      in your Keras config file at `~/.keras/keras.json`. If you never set it, then
      it will be "channels_last".
    dtype:
    - float
    - int
    - numeric
    - tf.bool
    - tf.dtype
    - tf.string
    enum:
    - (default)
    - channels_first
    - channels_last
    ndim:
    - '0'
    - '1'
    - '4'
    range:
    - '[0,inf)'
    shape:
    - '[2]'
    - '[batch,channels,height,width]'
    - '[batch,height,width,channels]'
    tensor_t:
    - tf.tensor
inputs:
  optional:
  - data_format
  - '**kwargs'
  required: []
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/layers/GlobalAveragePooling2D
package: tensorflow
target: GlobalAveragePooling2D
title: tf.keras.layers.GlobalAveragePooling2D
version: 2.1.0
