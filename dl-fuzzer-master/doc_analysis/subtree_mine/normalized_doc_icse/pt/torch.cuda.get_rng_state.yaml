constraints:
  device:
    default: cuda
    descp: 'The device to return the RNG state of. Default: `''cuda''` (i.e., `torch.device(''cuda'')`,
      the current CUDA device).'
    doc_dtype: torch.device or int, optional
    normalized_default: DEFAULT DF_STR
    normalized_descp:
    - The device to return the RNG state of
    - Default QSTR i e torch device QSTR the current CUDA device
    normalized_docdtype: torch device or D_TYPE optional
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/stable/cuda.html#torch.cuda.get_rng_state
package: torch
target: get_rng_state
title: torch.cuda.get_rng_state
version: 1.5.0
