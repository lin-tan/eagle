constraints:
  input:
    descp: the input tensor of size `(*, n, n)` where `*` is zero or more batch dimensions.
    doc_dtype: Tensor
    normalized_descp:
    - the input D_STRUCTURE of size BSTR where is zero or more batch dimensions
    normalized_docdtype: ONE_WORD D_STRUCTURE
inputs:
  optional: []
  required:
  - input
link: https://pytorch.org/docs/stable/torch.html#torch.det
package: torch
ret_type: Tensor
target: det
title: torch.det
version: 1.5.0
