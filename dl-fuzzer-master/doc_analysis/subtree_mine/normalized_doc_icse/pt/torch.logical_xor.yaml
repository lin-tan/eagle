constraints:
  input:
    descp: the input tensor.
    doc_dtype: Tensor
    normalized_descp:
    - the input D_STRUCTURE
    normalized_docdtype: ONE_WORD D_STRUCTURE
  other:
    descp: the tensor to compute XOR with
    doc_dtype: Tensor
    normalized_descp:
    - the D_STRUCTURE to compute XOR with
    normalized_docdtype: ONE_WORD D_STRUCTURE
  out:
    default: None
    descp: the output tensor.
    doc_dtype: Tensor, optional
    normalized_default: DEFAULT None
    normalized_descp:
    - the output D_STRUCTURE
    normalized_docdtype: D_STRUCTURE optional
inputs:
  optional:
  - out
  required:
  - input
  - other
link: https://pytorch.org/docs/stable/torch.html#torch.logical_xor
package: torch
ret_type: Tensor
target: logical_xor
title: torch.logical_xor
version: 1.5.0
