constraints:
  device:
    default: None
    descp: device for which to return the name. This function is a no-op if this argument
      is a negative integer. It uses the current device, given by `current_device()`,
      if `device` is `None` (default).
    doc_dtype: torch.device or int, optional
    normalized_default: DEFAULT None
    normalized_descp:
    - device for which to return the name
    - This function is a no op if this argument is a negative D_TYPE
    - It uses the current device given by current_device if QSTR is QSTR BSTR
    normalized_docdtype: torch device or D_TYPE optional
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/stable/cuda.html#torch.cuda.get_device_name
package: torch
target: get_device_name
title: torch.cuda.get_device_name
version: 1.5.0
