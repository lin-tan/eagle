constraints:
  module:
    descp: module containing the tensor to prune.
    doc_dtype: nn.Module
    normalized_descp:
    - module containing the D_STRUCTURE to prune
    normalized_docdtype: nn Module
  name:
    descp: parameter name within `module` on which pruning will act.
    doc_dtype: str
    normalized_descp:
    - parameter name within PARAM on which pruning will act
    normalized_docdtype: ONE_WORD D_TYPE
inputs:
  optional: []
  required:
  - module
  - name
link: https://pytorch.org/docs/stable/nn.html#torch.nn.utils.prune.identity
package: torch
target: identity
title: torch.nn.utils.prune.identity
version: 1.5.0
