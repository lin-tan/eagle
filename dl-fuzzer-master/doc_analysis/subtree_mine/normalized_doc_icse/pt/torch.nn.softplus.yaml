constraints:
  beta:
    default: '1'
    descp: 'the  beta  value for the Softplus formulation. Default: 1'
    normalized_default: DEFAULT CONSTANT_NUM
    normalized_descp:
    - the beta value for the Softplus formulation
    - Default CONSTANT_NUM
  threshold:
    default: '20'
    descp: 'values above this revert to a linear function. Default: 20'
    normalized_default: DEFAULT CONSTANT_NUM
    normalized_descp:
    - values above this revert to a linear function
    - Default CONSTANT_NUM
inputs:
  optional:
  - beta
  - threshold
  required: []
link: https://pytorch.org/docs/stable/nn.html#torch.nn.Softplus
package: torch
target: Softplus
title: torch.nn.Softplus
version: 1.5.0
