aliases:
- tf.compat.v1.sparse.eye
constraints:
  dtype:
    default: tf.dtypes.float32
    descp: The type of element in the resulting `Tensor`.
    normalized_default: D_TYPE D_TYPE
    normalized_descp:
    - The type of element in the resulting D_STRUCTURE
  name:
    default: None
    descp: A name for this `Op`. Defaults to "eye".
    normalized_default: DEFAULT None
    normalized_descp:
    - A name for this QSTR
    - Defaults to QSTR
  num_columns:
    default: None
    descp: Optional non-negative integer or `int32` scalar `tensor` giving the number
      of columns in the resulting matrix. Defaults to `num_rows`.
    normalized_default: DEFAULT None
    normalized_descp:
    - Optional non negative D_TYPE scalar D_STRUCTURE giving the number of columns
      in the resulting matrix
    - Defaults to PARAM
  num_rows:
    descp: Non-negative integer or `int32` scalar `tensor` giving the number of rows
      in the resulting matrix.
    normalized_descp:
    - Non negative D_TYPE scalar D_STRUCTURE giving the number of rows in the resulting
      matrix
inputs:
  optional:
  - num_columns
  - dtype
  - name
  required:
  - num_rows
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/sparse/eye
outputs: A `SparseTensor` of shape [num_rows, num_columns] with ones along the diagonal.
package: tensorflow
target: eye
title: tf.sparse.eye
version: 2.1.0
