aliases:
- tf.compat.v1.fake_quant_with_min_max_args
- tf.compat.v1.quantization.fake_quant_with_min_max_args
constraints:
  inputs:
    descp: A `Tensor` of type `float32`.
    normalized_descp:
    - A D_STRUCTURE of type D_TYPE
  max:
    default: '6'
    descp: An optional `float`. Defaults to `6`.
    normalized_default: DEFAULT CONSTANT_NUM
    normalized_descp:
    - An optional D_TYPE
    - Defaults to CONSTANT_NUM
  min:
    default: '-6'
    descp: An optional `float`. Defaults to `-6`.
    normalized_default: DEFAULT CONSTANT_NUM
    normalized_descp:
    - An optional D_TYPE
    - Defaults to CONSTANT_NUM
  name:
    default: None
    descp: A name for the operation (optional).
    normalized_default: DEFAULT None
    normalized_descp:
    - A name for the operation BSTR
  narrow_range:
    default: 'False'
    descp: An optional `bool`. Defaults to `False`.
    normalized_default: DEFAULT CONSTANT_BOOL
    normalized_descp:
    - An optional D_TYPE
    - Defaults to CONSTANT_BOOL
  num_bits:
    default: '8'
    descp: An optional `int`. Defaults to `8`.
    normalized_default: DEFAULT CONSTANT_NUM
    normalized_descp:
    - An optional D_TYPE
    - Defaults to CONSTANT_NUM
inputs:
  optional:
  - min
  - max
  - num_bits
  - narrow_range
  - name
  required:
  - inputs
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/quantization/fake_quant_with_min_max_args
outputs: A `Tensor` of type `float32`.
package: tensorflow
target: fake_quant_with_min_max_args
title: tf.quantization.fake_quant_with_min_max_args
version: 2.1.0
